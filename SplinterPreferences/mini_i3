# virtualbox needs
sudo apt install virtualbox-guest-dkms virtualbox-dkms virtualbox-guest-x11

# you possibly need to change keyboard layout with
setxkbmap

# i3WM
sudo apt install i3 i3blocks

# slim login manager
sudo apt install slim

# graphics
sudo apt install xorg

# feel like neo with cmatrix
sudo apt install cmatrix

# see OS info with
sudo apt install neofetch

# youre gonna need a browser
sudo apt install firefox

# and if you want sublime
wget -qO - https://download.sublimetext.com/sublimehq-pub.gpg | sudo apt-key add -
echo "deb https://download.sublimetext.com/ apt/stable/" | sudo tee /etc/apt/sources.list.d/sublime-text.list
sudo apt update
sudo apt install sublime

# midnight commander gives you a files browser but you might want nemo to have clicable windows
sudo apt install mc

# to change screen background
sudo apt install feh

# to customize icons install font awesome
sudo apt install fonts-font-awesome

# to connect with my bluetooth headset i needed
sudo apt install bluez pulseaudio-module-bluetooth

# restarting pulseaudio might be needed
pulseaudio -k
pulseaudio --start

# inside bluetoothctl, besides the obvious, I needed to run 'default-agent'
